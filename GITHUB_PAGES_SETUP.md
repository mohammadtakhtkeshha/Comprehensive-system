# 🚀 راهنمای کامل Deploy به GitHub Pages

## ✅ تنظیمات انجام شده

پروژه شما برای GitHub Pages آماده است و در آدرس زیر در دسترس خواهد بود:
**https://mohammadtakhtkeshha.github.io/Comprehensive-system/**

## 📋 مراحل تنظیم در GitHub

### مرحله 1: فعال‌سازی GitHub Pages

1. به repository خود بروید: https://github.com/mohammadtakhtkeshha/Comprehensive-system
2. روی **Settings** (تنظیمات) کلیک کنید
3. در منوی سمت چپ، **Pages** را انتخاب کنید
4. در بخش **Source**:
   - **Source** را روی **GitHub Actions** تنظیم کنید
   - **Branch** را روی **gh-pages** قرار دهید (اگر نیاز بود)
5. روی **Save** کلیک کنید

### مرحله 2: فعال‌سازی GitHub Actions

1. در همان صفحه Settings
2. در منوی سمت چپ، **Actions** → **General** را انتخاب کنید
3. در بخش **Workflow permissions**:
   - **Read and write permissions** را انتخاب کنید
4. روی **Save** کلیک کنید

## 🔧 Deploy خودکار

بعد از تنظیمات بالا، هر بار که به branch `main` push کنید، سایت به صورت خودکار deploy می‌شود.

### مراحل Deploy:

```bash
# 1. تغییرات را اضافه کنید
git add .

# 2. Commit کنید
git commit -m "آماده‌سازی برای GitHub Pages"

# 3. Push کنید
git push origin main
```

### بررسی وضعیت Deploy:

1. به repository بروید
2. تب **Actions** را باز کنید
3. workflow **Deploy to GitHub Pages** را مشاهده کنید
4. منتظر بمانید تا کامل شود (معمولاً 2-5 دقیقه)

## 🎯 تنظیمات فعلی پروژه

### ✅ تنظیمات Docusaurus

- **URL**: `https://mohammadtakhtkeshha.github.io`
- **Base URL**: `/Comprehensive-system/`
- **Organization**: `mohammadtakhtkeshha`
- **Project Name**: `Comprehensive-system`

### ✅ Workflow موجود

فایل `.github/workflows/deploy.yml` آماده است و:
- هنگام push به `main` اجرا می‌شود
- سایت را build می‌کند
- به GitHub Pages deploy می‌کند

## 🚨 رفع مشکلات

### مشکل: Actions اجرا نمی‌شود

**راه‌حل:**
1. به Settings → Actions → General بروید
2. مطمئن شوید که "Allow all actions and reusable workflows" فعال است

### مشکل: Deploy موفق نیست

**راه‌حل:**
1. به تب Actions بروید
2. روی آخرین workflow کلیک کنید
3. خطاها را بررسی کنید
4. معمولاً مشکل از npm install یا build است

### مشکل: سایت 404 نشان می‌دهد

**راه‌حل:**
1. مطمئن شوید که `baseUrl` در `docusaurus.config.js` درست است: `/Comprehensive-system/`
2. چند دقیقه صبر کنید (GitHub Pages گاهی تاخیر دارد)
3. Cache مرورگر را پاک کنید

### مشکل: تصاویر یا CSS لود نمی‌شوند

**راه‌حل:**
1. مطمئن شوید که همه فایل‌های static در `static/` هستند
2. از `/img/filename.svg` استفاده کنید (نه `img/filename.svg`)
3. بعد از تغییرات، دوباره deploy کنید

## 📝 Deploy دستی (اگر نیاز بود)

اگر می‌خواهید به صورت دستی deploy کنید:

```bash
cd 01-getting-starter
npm install
npm run build
npm run deploy
```

این دستور:
- سایت را build می‌کند
- به branch `gh-pages` push می‌کند
- سایت در GitHub Pages در دسترس خواهد بود

## ✅ چک‌لیست قبل از Deploy

- [ ] همه تغییرات commit شده‌اند
- [ ] `docusaurus.config.js` درست تنظیم شده
- [ ] `baseUrl` درست است: `/Comprehensive-system/`
- [ ] GitHub Pages در Settings فعال است
- [ ] GitHub Actions permissions درست است
- [ ] همه تصاویر و فایل‌های static موجود هستند

## 🎉 بعد از Deploy موفق

سایت شما در آدرس زیر قابل مشاهده خواهد بود:
**https://mohammadtakhtkeshha.github.io/Comprehensive-system/**

### زمان انتظار

- اولین deploy: 5-10 دقیقه
- Deploy‌های بعدی: 2-5 دقیقه

### بررسی سایت

1. به آدرس سایت بروید
2. بررسی کنید که همه صفحات لود می‌شوند
3. بررسی کنید که تصاویر نمایش داده می‌شوند
4. بررسی کنید که لینک‌ها کار می‌کنند

## 📞 کمک بیشتر

اگر مشکل دارید:
1. به تب Actions در GitHub بروید
2. خطاها را بررسی کنید
3. Logs را مطالعه کنید
4. تنظیمات را دوباره بررسی کنید

## 🔄 به‌روزرسانی سایت

برای به‌روزرسانی سایت:
1. تغییرات را در کد اعمال کنید
2. Commit و push کنید
3. منتظر بمانید تا Actions اجرا شود
4. سایت به‌روزرسانی می‌شود

---

**موفق باشید! 🚀**

